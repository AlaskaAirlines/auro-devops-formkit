name: Dev Demo

on:
  push:
    branches:
      - dev
  pull_request:

jobs:
  action:
    uses: AlaskaAirlines/auro-actions/.github/workflows/dev-demo.yml@dev
    with:
      publish-dir: "./auro-formkit-demo"
      build-command: "npm run gh-demo"